# Hourly_sensor_aggregation
this project processes hourly electricity consumption data using Python and Pandas, calculates average power consumption for each hour, and stores the aggregated results in Apache HBase and MongoDB.
